---
# Minimal packages for bootstrap testing
# Full lists in packages-full.yml
packages:
  - base
  - base-devel
  - git
  - zsh
  - jq
  - tree

# Skip AUR in dev/testing — too slow to build from source
aur_packages: []
